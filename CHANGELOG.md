# 更新记录

## v0.5.7

- 按实际接线修正位移电机1引脚：GPIO4=PWMA、GPIO5=AIN2、GPIO6=AIN1。
- 直通诊断程序同步升级为v1.2并修正A通道引脚。

## v0.5.6

- 恢复旧版工作程序中的GPIO17 TB6612 STBY控制。
- 上电时主动将GPIO17设为HIGH，唤醒两块TB6612驱动板。
- 直通诊断程序同步升级为v1.1并主动拉高STBY。

## v0.5.5

- 修复启动前应变已处于target时，程序立即跳过位移动作进入按摩的问题。
- 上电校准后必须连续300ms处于free状态，才允许两台位移电机开始夹紧。
- 如果应变片仍被按压，系统保持waiting，释放压力后自动开始。

## v0.5.4

- 修复压力夹紧状态绕过位移电机起动增力的问题。
- 两台位移电机现在同时以PWM 255起动800ms，再同时降到PWM 200。
- 两台位移电机的反向夹紧和target停止逻辑保持不变。

## v0.5.3

- 无压力且未达到目标时，两台位移电机同时按原方向的反方向运行。
- 压力达到target后，两台位移电机同时停止，再启动按摩电机。
- 安全释放方向同步改为与夹紧方向相反。

## v0.5.2

- 串口每行应变数据增加系统状态，持续显示waiting/clamping/massaging/releasing等状态。
- 达到目标压力后取消按摩启动等待，按摩电机1以PWM 255起动500ms后降到200。
- 按摩电机2继续延迟300ms错峰起动，降低同时起动电流冲击。

## v0.5.1

- 临时将过压阈值调整为3800，避免当前实测约3100的读数一启动就触发释放。
- 该值仅用于当前架空测试，后续应按实际安全压力重新标定。

## v0.5.0

- 增加压力触发状态流程：等待/夹紧/按摩/释放/完成/故障。
- 夹紧阶段仅运行位移电机；`delta`达到1000后才启动按摩电机。
- `delta`达到2500或夹紧10秒未达到目标时，反向释放1秒并停机。
- 按摩阶段最多运行60秒，完成后反向释放并停机。

## v0.4.3

- 根据实测delta约300～805调整临时阈值。
- 接触/目标/过载阈值改为250/600/1200。

## v0.4.2

- 根据实测确认应变模块为反向输出：无载约4095，受力后下降至约650。
- delta改为无载零点减去滤波值，不再取双向绝对值。
- 临时阈值调整为接触500、目标2000、过载3200。

## v0.4.1

- 上电前1.5秒自动计算应变无载零点。
- 增加相对零点的delta值和free/contact/target/over四级判断。
- 默认阈值为80、300、800，暂不直接控制电机。

## v0.4.0

- 新增GPIO14应变模块AO采集。
- 增加20ms采样、低通滤波以及串口最小值/最大值输出。
- 应变数据暂不参与电机控制，等待实测标定。

## v0.3.1

- 为B通道GPIO9/10/11按摩电机增加独立启动参数。
- B通道额外延迟300ms，满功率启动250ms后以210占空比持续运行。
- 其他三台电机参数保持不变。

## v0.3.0

- 扩展为四电机异步控制。
- 新增D通道位移电机：PWMD/DIN2/DIN1对应GPIO45/48/47。
- 新增B通道按摩电机：PWMB/BIN2/BIN1对应GPIO9/10/11。
- 两台位移电机协调往复，两台按摩电机持续相向旋转。
- 四台电机保留分组错峰启动、短行程和60秒自动停止。

## v0.2.3

- 位移电机改为250ms满功率启动、随后降到200占空比。
- 单方向位移时间从4秒缩短为1秒，换向停顿增加到600ms。
- 按摩电机延迟500ms启动，降低两台电机同时启动的电流冲击。

## v0.2.2

- 位移电机目标PWM由150提高到255，解决带机械负载时无法起转的问题。
- 按摩电机速度保持不变。

## v0.2.1

- 增加双电机100%占空比硬件诊断程序。
- 诊断程序绕过LEDC PWM和异步状态机，用于排查供电、STBY和驱动通道。

## v0.2.0

- 区分位移电机和按摩电机。
- 改用基于 `millis()` 的非阻塞异步状态机。
- 位移电机独立往复，按摩电机持续运行。
- 增加缓启动、换向停顿和60秒自动停止。

## v0.1.0

- 增加ESP32-S3双电机同步自动正反转测试。
