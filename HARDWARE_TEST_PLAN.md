# CloudLift ESP32 硬件联调与验收清单

> 未完成第 1～6 阶段前，设备不得接触人体。每完成一项后记录实测结果和最终参数。

## 1. 断电检查

- 两块 TB6612 的 `VCC` 接 3.3V，`VM` 接独立稳定 3.0V 电机电源。
- ESP32、两块 TB6612、压力放大板和电机电源共地。
- 每台 N20 单独占用一个 TB6612 通道，共使用两块驱动板。
- GPIO34、GPIO35 各有 10kΩ 上拉到 3.3V；常闭限位开关接 GPIO 与 GND。
- 电机电源入口有保险丝/限流；每块驱动板附近有 470～1000µF 电解电容。
- 摩擦轮机构架空，电机输出不会夹到手或线缆。

## 2. 上电静止检查

验收标准：ESP32 上电后四台电机均静止，只有按下按钮或收到 `START` 才能动作。

串口应输出：

```text
Cloud-Parting Hand controller ready
```

记录：

- 3.0V 电机电源空载电压：____ V
- ESP32 3.3V 电压：____ V
- TB6612 待机时是否发热：是 / 否

## 3. 单电机方向检查

先拆开机械负载或架空机构，逐一验证：

- 左弧形电机正方向应使左机构靠近。
- 右弧形电机正方向应按镜像结构靠近。
- 左右摩擦轮在组合/揉捻模式下应产生设计要求的相对旋转。
- 弧形往复时两侧机构应同步沿同一方向移动，不应互相顶紧。

方向不正确时，只修改 `firmware/include/config.h` 中对应 `*_SIGN`，不要交换多组软件定义。

## 4. 限位与断线检查

- 正常闭合时串口 `limits=0,0`。
- 手动按下左限位时左限位变为 1，往复方向必须离开左端。
- 手动按下右限位时右限位变为 1，往复方向必须离开右端。
- 拔掉任一限位线，应与触发限位具有相同效果。
- 夹紧过程中出现任一限位，应进入 `fault=limit_during_clamp`。

## 5. 压力采集与标定

压力放大板型号未确定前，本阶段不验收、不得接触人体。

确定放大板后记录每一路：

- 无载原始值：左 ____，右 ____
- 轻载原始值：左 ____，右 ____
- 目标压力原始值：1档 ____ / 2档 ____ / 3档 ____ / 4档 ____ / 5档 ____
- 绝对安全上限原始值：____
- 传感器断线时读数和软件判据：____

标定后更新 `targetPressure()` 和 `PRESSURE_MAX_RAW`。未标定的默认数值不是人体安全值。

## 6. BLE 与故障保护

使用 APP 或通用 BLE 调试工具按 `BLE_PROTOCOL.md` 测试：

- 能发现广播名 `CloudLift`。
- 能写入命令特征并订阅状态通知。
- `START`、`PAUSE`、继续和 `STOP` 状态正确。
- 修改力度、速度、时间后，状态包回显一致。
- 运行中关闭手机蓝牙，设备必须停止按摩并执行释放。
- 模拟超压，设备进入释放且上报 `over_pressure`。
- 故障后不能自动重启，排除问题后使用 `CLEAR_FAULT`。

## 7. 四种模式验收

- 模式0 组合：先夹紧，然后弧形往复与左右摩擦轮同时运行。
- 模式1 提拉：夹紧达到目标压力后，四台电机停止并由减速机构保持。
- 模式2 揉捻：夹紧后弧形电机停止，仅两台摩擦轮运行。
- 模式3 往复：夹紧后摩擦轮停止，仅两台弧形电机往复。
- 所有模式达到时间后均自动释放并回到 `idle`。

## 8. 带载但不接触人体

- 使用软质仿真材料代替皮肤，逐级测试力度1～5。
- 测量每台 N20 正常电流和堵转电流。
- 检查 TB6612、电机、电池和线缆温升。
- 验证最坏情况下保险丝/限流和硬件急停有效。
- 连续运行 30 分钟，无复位、异常换向、丢失限位或驱动过热。

## 9. 人体测试前置条件

- 压力已用可追溯的力值完成标定，而非凭原始 ADC 数字估计。
- 已实现并验证传感器断线、短路和读数冻结检测。
- 有独立于 ESP32 软件的硬件急停和电源切断装置。
- 堵转电流在驱动器、电池和线材安全范围内。
- 已完成风险评估、禁忌筛查和测试人员知情同意流程。
