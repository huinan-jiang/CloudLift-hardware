# CloudLift 本地/国内开发环境部署方案

硬件端采用本地离线部署：ESP32 固件烧录后完全离线运行，不依赖境外服务器、国内云或手机网络。

## 推荐目录

为避免 PlatformIO 对中文路径的兼容问题，将仓库放在纯英文短路径，例如：

```text
C:\CloudLift
```

工程的 `platformio.ini` 已将工具链固定到仓库根目录的 `.pio-core`。第一次联网安装完成后，可以把整个 `C:\CloudLift` 文件夹复制到其他 Windows 电脑继续使用。

## 两个固件工程

- `motor_test`：无传感器空载试转，当前阶段优先使用。
- `firmware`：正式 BLE、安全状态机版本，安装压力放大板并完成标定后使用。

## 首次联网准备

1. 安装 VS Code 和 PlatformIO IDE。
2. 把仓库放到 `C:\CloudLift`。
3. 打开 `motor_test` 文件夹并执行 Build；PlatformIO 会把 ESP32 平台、编译器和烧录工具下载到 `C:\CloudLift\.pio-core`。
4. Build 成功后断网再执行一次 Build，确认离线环境完整。
5. 将完整仓库备份为离线开发包，不要删除 `.pio-core`。

PlatformIO 官方说明 `core_dir` 保存平台、工具链、框架和全局依赖，并支持将本地文件夹或压缩包作为软件包来源。因此完整缓存可以在项目内保存和迁移。

## 国内网络建议

- 优先在一台网络稳定的电脑上完成首次下载，再复制离线包；不要在每台电脑重复下载。
- 不建议把未经核验的第三方“国内镜像”直接写进固件工程，避免工具链被替换或版本不一致。
- 正式版本固定 ESP32 平台版本，不自动升级。
- 当前固件已移除 ArduinoJson，减少一个外部下载依赖。

## 完全离线交付内容

最终离线包应包含：

- 源代码和协议文档
- `.pio-core` 完整工具链缓存
- 编译成功的 `.bin` 固件
- 烧录工具和烧录说明
- 固件版本、代码提交号和 SHA-256 校验值

在本机工具链首次成功安装前，`.pio-core` 仍是不完整缓存，不能作为正式离线包交付。
